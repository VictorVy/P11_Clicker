void gameOver()
{
  background(gameOverBG);
  
  textAlign(CENTER);
  fill(black);
  text("Your time was:", width / 2, height / 2 - 25);
  text(timeScore, width / 2, height / 2);
}

void gameOverMR()
{
  
}
